# 2048小游戏

## 暂未完成，完善中

```
# 1、 安装依赖
npm install or yarn install

# 2、 运行
npm run dev， 默认监听8080。

# 3、 打包
npm run build，默认在主目录下"dist"文件夹。

# 剩余问题：
1、每一列或一行有合并的元素后该行或列其余元素不会再合并
2、检测剩余格子的问题
3、计算分数、本地存储
4、重新开始
